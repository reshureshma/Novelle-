<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NOVELLE Admin Panel</title>
    <link rel="stylesheet" href="styles.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Arial', sans-serif;
        }

        body {
            background-color: #fff;
        }

        /* Header */
        .header {
            padding: 15px;
            border-bottom: 1px solid #e0e0e0;
            text-align: center;
        }

        .header h1 {
            font-size: 24px;
            color: #000;
            font-weight: bold;
            text-transform: uppercase;
        }

        /* Container */
        .container {
            display: flex;
            margin: 20px;
        }

        /* Sidebar */
        .sidebar {
            width: 320px;
            background-color: white;
            border: 1px solid #e0e0e0;
            border-radius: 8px;
            overflow: hidden;
        }

        .sidebar-item {
            display: flex;
            align-items: center;
            padding: 15px;
            border-bottom: 1px solid #e0e0e0;
            cursor: pointer;
            text-decoration: none;
            color: inherit;
            transition: background-color 0.2s ease;
        }

        .sidebar-item:hover,
        .sidebar-item.active {
            background-color: #002366;
            color: white;
        }

        .sidebar-item i {
            margin-right: 15px;
        }

        .sidebar-item span {
            font-size: 14px;
            font-weight: 500;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        /* Main Content */
        .main-content {
            flex-grow: 1;
            margin-left: 20px;
        }

        .panel-title {
            font-size: 36px;
            color: #002366;
            margin-bottom: 20px;
            font-weight: bold;
        }

        /* Top Controls */
        .top-controls {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        /* Search Bar */
        .search-bar input {
            width: 220px;
            padding: 10px 15px;
            border: 1px solid #e0e0e0;
            border-radius: 20px;
            font-size: 14px;
            outline: none;
        }

        /* Add Product Button */
        .add-product-btn {
            padding: 10px 15px;
            background-color: #002366;
            color: white;
            border: none;
            border-radius: 5px;
            font-size: 14px;
            font-weight: 500;
            cursor: pointer;
            transition: background-color 0.2s ease;
            text-transform: uppercase;
        }

        .add-product-btn:hover {
            background-color: #001845;
        }

        /* Product Table */
        .product-table {
            width: 100%;
            background-color: #f5f5f5;
            border-radius: 5px;
            overflow: hidden;
        }

        .product-table-header,
        .product-table-row {
            display: grid;
            grid-template-columns: 80px 2fr 1fr 1fr 1fr 1fr 1fr 1fr;
            padding: 15px;
            border-bottom: 1px solid #e0e0e0;
            align-items: center;
        }

        .product-table-header {
            background-color: #ddd;
            font-weight: bold;
            text-transform: uppercase;
        }

        .product-table-row:last-child {
            border-bottom: none;
        }

        .product-img {
            width: 60px;
            height: 80px;
            object-fit: cover;
            border-radius: 3px;
        }

        .badge {
            padding: 4px 8px;
            border-radius: 3px;
            font-size: 12px;
            font-weight: 500;
            color: white;
            text-align: center;
        }

        .badge.yes {
            background-color: #52c41a;
        }

        .badge.no {
            background-color: #f5222d;
        }

        /* Action Buttons */
        .update-btn, .delete-btn {
            padding: 6px 12px;
            border: none;
            border-radius: 3px;
            color: white;
            font-size: 12px;
            cursor: pointer;
            transition: background-color 0.2s ease;
            text-transform: uppercase;
            margin-left: 5px;
        }

        .update-btn {
            background-color: #1890ff;
        }

        .update-btn:hover {
            background-color: #40a9ff;
        }

        .delete-btn {
            background-color: #f5222d;
        }

        .delete-btn:hover {
            background-color: #ff4d4f;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>NOVELLE</h1>
    </div>
    
    <div class="container">
        <div class="sidebar">
            <a href="/admin/insights" class="sidebar-item"><i>📊</i><span>Insight</span></a>
            <a href="/admin/orders" class="sidebar-item"><i>📦</i><span>Order Management</span></a>
            <a href="/admin/users" class="sidebar-item"><i>👥</i><span>User Management</span></a>
            <a href="/admin/products" class="sidebar-item active"><i>🛒</i><span>Product Management</span></a>
            <a href="/admin/categories" class="sidebar-item"><i>📁</i><span>Category Management</span></a>
            <a href="/admin/coupons" class="sidebar-item"><i>🎟️</i><span>Coupon Management</span></a>
            <a href="/admin/logout" class="sidebar-item"><i>🚪</i><span>Logout</span></a>
        </div>
        
        <div class="main-content">
            <h2 class="panel-title">ADMIN PANEL</h2>

            <div class="top-controls">
                <div class="search-bar">
                    <input type="text" placeholder="Search Product">
                </div>
                <button class="add-product-btn">➕ Add New Product</button>
            </div>

            <div class="product-table">
                <div class="product-table-header">
                    <span>Image</span>
                    <span>Product Name</span>
                    <span>Category</span>
                    <span>Price</span>
                    <span>Stock</span>
                    <span>Edit</span>
                    <span>Block</span>
                    <span>Delete</span>
                </div>
                <div class="product-table-row">
                    <span><img src="product.jpg" class="product-img"></span>
                    <span>Classic White T-Shirt</span>
                    <span>Clothing</span>
                    <span>₹499</span>
                    <span class="badge yes">Yes</span>
                    <button class="update-btn">Edit</button>
                    <button class="delete-btn">Block</button>
                    <button class="delete-btn">Delete</button>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
